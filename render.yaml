services:
  - type: web
    name: scada-ai-system
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    autoDeploy: true
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: API_PORT
        value: 10000
      - key: LOG_LEVEL
        value: INFO
      - key: DATABASE_URL
        value: sqlite:///./data/scada.db
      - key: REDIS_ENABLED
        value: false
    healthCheckPath: /status
