services:
  - type: web
    name: scada-ai-system
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: LOG_LEVEL
        value: INFO
      - key: API_PORT
        value: 9000
      - key: DATABASE_URL
        value: sqlite:///./data/scada.db
    healthCheckPath: /health
